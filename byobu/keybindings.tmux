unbind-key -n C-a
set -g prefix ^A
set -g prefix2 F12
bind a send-prefix

unbind-key -n S-Up
unbind-key -n S-Down
unbind-key -n S-Left
unbind-key -n S-Right
unbind-key -n S-M-Up
unbind-key -n S-M-Down
unbind-key -n S-M-Right
unbind-key -n S-M-Left
unbind-key -n M-Up
unbind-key -n M-Down
